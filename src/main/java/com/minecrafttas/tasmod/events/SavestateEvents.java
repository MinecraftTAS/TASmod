package com.minecrafttas.tasmod.events;

import java.io.File;

/**
 * Events for savestating
 * @author Scribble
 *
 */
public class SavestateEvents {
	
	/**
	 * Executed when creating a savestate
	 * @param targetfolder Folder where the savestate will be saved to
	 */
	public static void triggerSavestateEvent(File targetfolder) {
		
	}

	/**
	 * Executed when loading a savestate
	 * @param targetfolder The folder where the savestate will be coming from
	 */
	public static void triggerLoadstateEvent(File targetfolder) {
		
	}
}
